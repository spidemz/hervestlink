services:
  - type: web
    name: harvestlink
    runtime: node
    buildCommand: ""
    startCommand: node server.js
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: harvestlink-db
          property: connectionString

databases:
  - name: harvestlink-db
    plan: free